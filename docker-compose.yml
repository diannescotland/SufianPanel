services:
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: sufian-panel-backend
    ports:
      - "8000:8000"
    volumes:
      - backend_data:/app/data
      - ./backend/media:/app/media
    environment:
      - SECRET_KEY=k1uic_pp0(elssiewv3j-_xvk8nt1ch7f2!tze62qvxe*nwaoe
      - DEBUG=True
      - CORS_ALLOWED_ORIGINS=http://localhost:3000
      - DJANGO_SUPERUSER_USERNAME=sufianbh26
      - DJANGO_SUPERUSER_EMAIL=sufianbh26@example.com
      - DJANGO_SUPERUSER_PASSWORD=Bigday2626@
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request; urllib.request.urlopen('http://localhost:8000/api/health/')"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s
    # Resource limits for better stability
    deploy:
      resources:
        limits:
          memory: 1G
        reservations:
          memory: 512M

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    container_name: sufian-panel-frontend
    ports:
      - "3000:3000"
    environment:
      - NEXT_PUBLIC_API_URL=http://localhost:8000/api
      - HOSTNAME=0.0.0.0
      - NODE_ENV=production
    depends_on:
      backend:
        condition: service_healthy
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "wget", "-q", "--spider", "http://localhost:3000"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 60s
    # Resource limits for better stability
    deploy:
      resources:
        limits:
          memory: 1G
        reservations:
          memory: 512M

volumes:
  backend_data:
